name: sanjoyapp_print
description: 三joy打印中转App - 接收Web端打印数据，通过BLE发送到标签打印机
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dependencies:
  flutter:
    sdk: flutter
  # 移除：flutter_blue_plus: ^1.32.0
  flutter_reactive_ble: ^5.0.2
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.6
  # 加上平台检测
  universal_ble: ^0.1.0  # 可选，用于统一接口

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
